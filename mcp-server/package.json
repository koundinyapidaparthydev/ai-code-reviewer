{
  "name": "ai-code-reviewer-mcp-server",
  "version": "1.0.0",
  "private": true,
  "description": "Local stdio MCP server exposing read_file, grep, and list_files over a workspace root.",
  "main": "dist/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "types": "tsc --noEmit"
  },
  "dependencies": {
    "ts-node": "^10.9.2",
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/node": "^20.10.5"
  }
}
